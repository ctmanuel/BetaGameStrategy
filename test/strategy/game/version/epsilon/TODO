X	-2 Lieutenants
X	-2 first Lieutenants
X		-only move one square at a time
X		-can attack from 2 squares away
X			-cannot attack if piece in the way
X			-win: move to defending piece's spot
X			-lose: defending pieces don't move
X		-Lieutenant vs. first Lieutenant, both removed
X	-2 flags
X		-flags replace any piece of player's choosing
X	-game ends when both flags captured
X		-first flag captured, status = FLAG_CAPTURED
X		-second flag captured, status = WIN
X	-resign
X		-move with null configuration
X		-opponent wins game
X	-draw condition with 2 flags

		
	-add observer to factory call
	-2% code coverage